# Custom user secrets template
# This should NOT be overwritten

version: "2.0"
secrets:
  - name: aws-creds
    fields:
    - name: aws_access_key_id
      value: "An aws access key"

    - name: aws_secret_access_key
      value: "An aws access secret key"
